let config = {
    token:"your-token-account",//You need to put an account token here, if u put an bot account it doesnt work
    botPrefix:"bot!",//the prefix por your selfbot
    guildID:'724404552014626866',//these is the id of the guild where u want to put the autocatcher
    anigamePrefix:"."//the prefix of the anigame bot
}

module.exports = config;